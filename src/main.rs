fn fun_calc(x: i32, y: i32) -> i32 {
    x + y
}

fn main() {
    println!("my first rust!");
    println!("commit branches");
    let x = 1;
    let y = 2;
    println!("x + y = {}", fun_calc(x, y));
}
